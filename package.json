{
  "author": {
    "name": "Motionpicture co.,ltd."
  },
  "contributors": [
    {
      "name": "Tetsu Yamazaki",
      "email": "yamazaki@motionpicture.jp"
    }
  ],
  "dependencies": {
    "@types/mongoose": "4.7.5",
    "debug": "^2.6.4",
    "moment": "^2.18.1",
    "mongoose": "4.8.1",
    "numeral": "^2.0.6",
    "redis": "^2.7.1",
    "uniqid": "^4.1.1"
  },
  "description": "This is a CHEVRE domain repository.",
  "devDependencies": {
    "@types/debug": "0.0.29",
    "@types/mocha": "^2.2.41",
    "@types/numeral": "0.0.19",
    "@types/redis": "^0.12.36",
    "ink-docstrap": "^1.3.0",
    "jsdoc": "^3.4.3",
    "mocha": "^3.3.0",
    "nsp": "^2.6.3",
    "rimraf": "^2.6.1",
    "snyk": "^1.29.0",
    "tslint": "^4.5.1",
    "tslint-microsoft-contrib": "^4.0.1",
    "typescript": "^2.3.0"
  },
  "engines": {
    "node": ">=6.9.1",
    "npm": ">=3.10.8"
  },
  "keywords": [],
  "license": "UNLICENSED",
  "main": "./lib/index.js",
  "name": "@motionpicture/chevre-domain",
  "repository": {},
  "scripts": {
    "build": "tsc",
    "check": "tslint --type-check --project tsconfig.json -c tslint.json lib/**/*.ts && nsp check && snyk auth && snyk test --ignore-policy",
    "clean": "rimraf examples/**/*.js lib/**/*.js test/**/*.js types/**/*.d.ts npm-debug.log* docs/*",
    "jsdoc": "node_modules/.bin/jsdoc -c jsdoc.json",
    "prepublishOnly": "npm run clean && npm run build && npm test",
    "test": "npm run check && mocha --no-timeouts --recursive",
    "preversion": "npm run clean && npm run build && npm test",
    "version": "git add -A",
    "postversion": "git push origin --tags"
  },
  "types": "./types/lib/index.d.ts",
  "version": "6.0.0"
}
